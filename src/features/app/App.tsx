import {NumbersComponent} from '../editNumbers';
import {FooterComponent} from '../footer';

export const App = () => {

  return (
    <div>
      <NumbersComponent/>
      <FooterComponent/>
    </div>
  );
};
